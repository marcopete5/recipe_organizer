SECRET_KEY = ''


# Database
# https://docs.djangoproject.com/en/1.9/ref/settings/#databases
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "recipe_organizer",
        "USER": "user",
        "PASSWORD": "password",
        "HOST": "localhost",
        "PORT": "",
    }
}
